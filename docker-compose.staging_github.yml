version: "3.9"
services:
    front:
        image: ghcr.io/deltarm/cp1-front/github-staging
        container_name: cp1_staging_github
        restart: always
        ports:
            - 80:80
            - 443:443
        volumes:
            - /home/deltarm/etc/ssl:/etc/nginx/certs
