import React from 'react';
import { Button } from '@mui/material';

export const ReferentielControl: React.FC = ({}): React.ReactElement => {
  return (
    <Button style={{ backgroundColor: '#533fd0', color: 'white' }}>
      Embedded CP1 content referentiel control
    </Button>
  );
};
