import styled from 'styled-components/macro';

export const CustomCommentRendererStyled = styled.div`
  position: relative;

  .MuiSvgIcon-root:hover {
    color: inherit;
    cursor: inherit;
  }
`;
