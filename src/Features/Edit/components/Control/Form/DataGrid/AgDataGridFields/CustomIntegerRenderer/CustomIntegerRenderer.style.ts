import styled from 'styled-components/macro';

const CustomIntegerRendererStyled = styled.div.attrs((props: any) => props)`
  flex-direction: row;
  align-items: center;
`;

export { CustomIntegerRendererStyled };
