import styled from 'styled-components/macro';

const CustomPercentRendererStyled = styled.div.attrs((props: any) => props)`
  flex-direction: row;
  align-items: center;
`;

export { CustomPercentRendererStyled };
