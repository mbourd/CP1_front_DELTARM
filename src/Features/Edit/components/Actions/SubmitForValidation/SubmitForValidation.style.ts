import styled from 'styled-components/macro';

export const SubmitForValidationStyled = styled.div`
  display: inline-block;
`;

export const ModalFooterStyled = styled.div`
  padding: ${({ theme }) => theme.spacing.normal};
`;
