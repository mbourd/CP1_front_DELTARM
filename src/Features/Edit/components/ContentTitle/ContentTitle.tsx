import React from 'react';
import { ContentTitleStyled } from './ContentTitle.style';

export const ContentTitle: React.FC = (): React.ReactElement => {
  return (
    <ContentTitleStyled>
      <p>ContentTitle.tsx</p>
    </ContentTitleStyled>
  );
};
