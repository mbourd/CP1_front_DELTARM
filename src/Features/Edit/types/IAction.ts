export interface IApiAction {
  action_lib: string;
  id_action: string;
  route: string;
  action_code: string;
}

export interface IAction {
  label: string;
  id: string;
  url: string;
  code: string;
}
